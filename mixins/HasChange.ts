const defaultProps = {
    onChange: {
      required: false,
      type: [Function],
      default: null,
      // private: true, // Handled by convention in React
    },
  };
  
  export default defaultProps;