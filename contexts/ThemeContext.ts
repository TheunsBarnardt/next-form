/* eslint-disable @typescript-eslint/no-explicit-any */
// src/contexts/ThemeContext.ts
import { createContext } from 'react';

export const ThemeContext = createContext<any | undefined>(undefined); // Adjust type as needed